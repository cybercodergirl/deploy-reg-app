version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      args:
        - REACT_APP_API_URL=http://52.71.159.85:9091  # haproxy server public ip address and port
    ports:
      - "9090:3000"
    environment:
      - REACT_APP_API_URL=http://52.71.159.85:9091 # haproxy server public ip address and port
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "9091:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/magax
      - SECRET_KEY=${{ secrets.JWT_SECRET_KEY }}
    depends_on:
      - db

  db:
    image: postgres:13
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=magax
    ports:
      - "9092:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: 